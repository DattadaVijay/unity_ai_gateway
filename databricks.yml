# This is a Declarative Automation Bundle definition for databricks_ai_gateway.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: databricks_ai_gateway
  uuid: 200e1a72-ca17-464f-90df-c9e84f10b714

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-e3761da8-7d95.cloud.databricks.com
    variables:
      catalog: main
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-e3761da8-7d95.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/dattada.vijay@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/dattada.vijay@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: main
      schema: prod
    permissions:
      - user_name: dattada.vijay@gmail.com
        level: CAN_MANAGE
